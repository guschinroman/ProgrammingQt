#include "fraction.h"

Fraction::Fraction()
{

}

bool Fraction::isEqual(const Fraction f) const
{
    return this->fNumerator == f.fNumerator
            && this->fDenominator == f.fDenominator; 
}

void Fraction::setDenominator(int value )
{
    this->fDenominator = value;
}